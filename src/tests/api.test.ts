import { describe, it, expect } from "vitest";
// - import { getQueriesForElement } from "@testing-library/dom";

describe("Library", () => {
    describe("API", () => {
        it("is consistent", () => {
            expect(1).toEqual(1);
        });
    });
});
